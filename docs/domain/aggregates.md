# Aggregates

- Registry owns registered checks and duplicate prevention.
- Check owns its DSL metadata and produced results.
- Runner owns one execution of registry, context, reporting, and exit code.
