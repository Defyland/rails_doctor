# Module Boundaries

- `Check`: DSL and result creation.
- `Registry`: check collection and duplicate detection.
- `Context`: Rails application inspection boundary.
- `Reporter`: output rendering only.
- `Runner`: command orchestration and exit code policy.
- `checks/*`: built-in diagnostics grouped by Rails concern.
