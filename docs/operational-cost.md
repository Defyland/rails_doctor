# Operational Cost

RailsDoctor adds no running infrastructure. Its cost is command execution time in
CI or deploy workflows.

The main maintenance cost is check accuracy across Rails versions and deployment
topologies. Stable check IDs, structured evidence, time-bounded auditable
suppressions, proactive expiry warnings, low-noise command-hook defaults, and
integration tests are the planned controls.
